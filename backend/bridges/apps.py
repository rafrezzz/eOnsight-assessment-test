from django.apps import AppConfig


class BridgesConfig(AppConfig):
    """
    Configuration class for the bridges app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'bridges'
